# 更新日志

## [Unreleased]

### 新增

- 工作台重构为「对话 + 证据流程」两栏形态：主体是聊天式对话，右侧是可视化的确定性证据流水线（数据源 → 数据信号 → 文档检索 → 回到数据验证 → 结论），每步可展开查看「做了什么、得到什么」。
- 纯对话式分析：在输入框提问即触发确定性分析，确定性结论作为特殊标注的消息插入对话（助手不可覆盖）；`context.attached` 事件携带结构化步骤数据（source/metrics/analysis）驱动流水线渲染。
- 本地助手作为对话核心驱动，确定性引擎作为其证据工具；数据源配置收进可折叠的「数据源设置」面板。

- 数据接入层（`data2doc2data/ingestion.py`）：支持本地任意格式（CSV / TSV / JSON / XLSX）与数据 API（HTTPS 快照）两种来源，并把它们统一转换为 `date,metric,value` 标准指标行。接入层负责「想/理解」（格式探测、结构预览、字段映射建议），引擎保持确定性「证」（逐行校验、可溯源、Agent 不可篡改）。

- 声明式验证规则 DSL（`data2doc2data/rules.py`）：使用者可用本地 JSON 文件声明指标（别名、显示名、聚合、比较窗口、方向阈值、最小观测数、重复策略）与命名多指标规则，引擎据此裁决并标注命中的 `rule_id`/`rule_name`，不再局限于内置的两个指标。
- 分析流程改为由规则集驱动：别名解析、显示名、指标定义和文档条件验证均取自规则集；未声明规则时回退到与历史行为等价的内置默认规则集。
- CLI 新增 `analyze --rules <path>` 与 `check-rules --rules <path>`；工作区配置（Profile）新增可选 `rules_path`，保存配置时即校验规则文件。
- 证据契约版本化：每份证据快照与信封都会标注 `EVIDENCE CONTRACT v1`，`ContextSummary` 新增 `contract_version` 字段，供跨 harness 消费方（WorkBuddy/DeepSeek harness/Codex 插件）检测契约漂移。
- 文档检索新增静态同义词归一化（`retrieval.py`）：内置中文/英文业务同义词组（客户/用户、营收/收入、流失率/churn 等），在 BM25 评分前把同义词归一到规范形式，提升中文业务文档召回；`search_chunks` 支持 `synonyms` 参数可关闭，原始 chunk 文本始终原文保留、可追溯。

- 数据接入 HTTP 端点与服务端编排（`server.py`）：新增 `/api/ingest/upload`（本地文件 base64 上传）、`/api/ingest/preview`（结构预览 + 内置映射建议）、`/api/ingest/apply`（按确认方案转换并写入标准 CSV、回写 Profile 的 `data_path`）、`/api/ingest/api-snapshot`（HTTPS 拉取快照后本地预览）；`Profile` 扩展 `api` 模式与 `ingestion`/`api` 配置字段。
- 工作台新增「接入任意数据源」面板（`ingest-panel.js`）：本地文件（拖拽/选择后自动上传预览）与数据 API（填地址 + 可选请求头后拉取快照）两种入口，统一进入字段映射方案编辑器（日期/指标/数值/记录路径/工作表/日期格式，预填建议值），应用后即成为当前分析的确定性数据源；全程无 `innerHTML`、无外链，文件仅在本机解析。

- 接入层增强（上一版两项边界补齐）：
  - **Agent 在环提案**：新增 `/api/ingest/propose` 与前端「让助手帮我推断映射」按钮。助手只读取已探测出的字段名与样例数据（从不接触原始文件）来推断 `date/metric/value` 映射；无可用助手时自动回退到内置建议，用户永不被阻塞。提案与内置建议共用同一 `IngestionPlan` 契约，确认后执行仍由确定性引擎逐行校验。
  - **文档目录闭环**：`/api/ingest/apply` 现接受 `knowledge_path`（应用后回写 Profile），并捕获 `ingestion`（来源 + 方案 + 时间）与 `api`（API 模式下的端点配置）配置；当缺少文档目录时返回 `needs_knowledge_path` 与 `knowledge_warning`，前端以醒目样式提示「确定性结论需要它作为证据」，避免应用后分析因缺文档目录而失败。
  - **预览时自动请助手推断（use_agent）**：`/api/ingest/preview` 与 `/api/ingest/api-snapshot` 新增 `use_agent` 参数；前端「接入任意数据源」面板新增「预览时请助手自动推断映射」勾选框，文件选择／本机路径／API 快照三种入口触发预览时若勾选且存在可用助手，则直接预填助手给出的映射（无助手自动回退内置建议）。仍保留「让助手帮我推断映射」手动按钮。
  - **本机绝对路径输入（绕过大文件上传）**：本地文件入口新增「本机文件路径」输入框与「用本机路径预览」按钮；后端 `ingest_preview` 新增 `validate_local`，校验路径为绝对路径、存在、是常规文件、未超本地处理大小上限，错误给出明确中文提示，省去超大文件的 base64 上传。

### 变更

- 助手操作流按回合与事件类型聚合：连续 `plan.delta`、命令输出和工具结果更新同一张卡片，避免长任务生成数百张碎片卡；审批卡始终置顶并锁定操作区滚动位置，处理完成后自动清除待审批徽标。
- 对话区只在读者接近底部时自动跟随流式回复，主动向上阅读时保持当前位置；安全 Markdown 渲染新增标题、引用和嵌套列表，仍全程使用 `createElement`/`textContent`。
- 工作台紧凑控件统一采用至少 12px 辅助文字与 38px 点击高度，工具栏支持换行，长内容和窄窗口下更易读、更易操作。
- 数据 API 快照严格规范 HTTPS URL、查询参数与请求头；手动跟随最多 3 次 HTTPS 重定向，跨源时剥离认证信息，拒绝 URL 凭据、非 443 端口与危险请求头。
- 规则文件为受信任的数据契约，加载时严格校验（版本、指标名、规则 id、方向、聚合、阈值、重复指标集等），超过 256 KB 或格式错误返回明确本地错误。
- 助手适配器共享逻辑下沉到 `agents/_shared.py`（`emit_provider_error`、`validate_session`、`minimal_environment`、`required_text`），消除 Codex 与 WorkBuddy 之间的重复实现，并补充适配器协议测试（总覆盖率 84% → 86%，gateway 75% → 87%，codex 77% → 81%）。
- 前端从单一 `app.js` 拆分为零构建 ES modules（`state.js`/`ui.js`/`api.js`/`data-panel.js`/`analysis-panel.js`/`assistant-panel.js` + `app.js` 入口），`index.html` 改用 `type="module"`；本地服务白名单与发布打包器同步纳入全部模块，安全契约测试改为覆盖所有脚本文件。
- 新增跨 harness 工具接口（`data2doc2data mcp`）：零依赖的 MCP stdio 服务器，把确定性引擎暴露为 `analyze`、`check_rules`、`source_profile` 三个工具，任意 MCP 客户端（WorkBuddy/DeepSeek harness/Codex/通用客户端）可直接以工具方式调用，原始 CSV 永不写入客户端。

### 修复

- 浏览器会话过期或本地服务重启后，写请求遇到 403 会自动重新领取 Cookie/CSRF 授权并仅重试一次，不再要求使用者手动刷新页面；并发续签会复用同一个刷新结果。
- 导入写接口现与助手接口共用浏览器会话和 CSRF 校验，未授权页面无法上传、预览、应用或拉取 API 快照。
- WorkBuddy/CodeBuddy 对审批决定返回成功但无响应体时不再误报“提供方响应无效”；真实 2.115 环境中拒绝审批会正确落为“已拒绝”并清除待审批状态。
- 本地 Host 安全测试改用直接回环 HTTP 连接，不再受 macOS 系统代理配置影响而误报 502。
- 助手事件流改为断线自动重连：`onerror` 不再主动关闭 EventSource，而是利用浏览器的 `Last-Event-ID` 续传能力从断点恢复，仅在活动 turn 持续断线 15 秒时兜底提示，避免长对话因一次网络抖动而中断。
- 工作台首屏改为并行初始化（助手检测与工作区数据加载互不依赖），缩短启动等待。
- 修复 981–1180px 三栏横向溢出：调整网格列宽，消除窄桌面下右侧助手栏被裁剪的问题。
- WorkBuddy 适配 codebuddy 2.x 的 serve 认证：启动时从输出捕获一次性密码并以 `Authorization: Bearer` 头访问，修复此前 health 返回 `AUTH_REQUIRED` 导致无法连接的问题；启动命令指定 `--model hy3`。
- WorkBuddy 适配 codebuddy 2.x 的流式响应：消息增量（`agent_message_chunk` 等）由独立 GET SSE 流改为内联在 `session/prompt` 的 POST SSE 响应里，据此重写 `stream_turn` 直接从该流读取增量事件，修复助手只出确定性结论、不返回解释的问题。

### 变更

- 工作台数据栏新增「验证规则 JSON」入口，把声明式规则 DSL 的能力对网页使用者可见；保存时校验、分析时按规则裁决。
- 分析结果新增「分析历史」区块，保留多次分析并按验证状态标注；数据源切换后旧结论标记为「已失效（数据源已切换）」但保留可追溯。
- 证据上下文面板展示 `contract_version`；流式助手输出期间抑制 `aria-live` 播报，避免屏幕阅读器被逐字打断。
- 顶部状态条收敛为「数据源 + 隐私边界」，移除重复的分析/助手状态徽标；验证规则输入收进「高级选项」折叠，本地文件表单更精简。
- 助手显示名简化为 `codex` / `workbuddy`，移除「腾讯 WorkBuddy」前缀、版本号与状态后缀。
- 助手回答支持安全的 Markdown 渲染：加粗、行内代码、无序/有序列表与代码块用 `createElement` + `textContent` 渲染为 DOM，不再原样显示 `**` 等标记；不引入 `innerHTML`，安全边界不变。
- 证据流水线跟随对话回合：每个回合保存确定性分析快照，点击任意消息时右侧流水线切换到该回合的步骤，历史结论可追溯；非指标问题的回合不再显示上一轮分析。
- 审批请求到达时自动展开操作面板并显示「待审批」徽标，避免安全关键决策被折叠面板掩盖。

## [3.0.0] - 2026-08-17

### 新增

- 内置“增长质量预警”“策略与数据冲突”“证据不足”三套虚构合成数据场景，并在网页中提供可访问的场景选择、说明、学习目标和建议问题。
- 网页可直接检测并连接本机 Codex 与腾讯 WorkBuddy/CodeBuddy，展示流式对话、工具输出、文件差异、审批和中断状态。
- 新增数据、确定性分析与助手协作三栏工作台，并在移动端提供可访问的区域标签切换。
- 助手每轮自动附带服务端生成的有界证据上下文：本地统计、匹配的确定性结论和问题相关文档片段；原始 CSV 不进入提示词，超出预算时自动压缩。
- 新增只读、协作和信任本次会话权限模式，以及会话归属、CSRF、批准过期、工作区限制、审计脱敏和子进程清理。

### 变更

- 确定性分析支持受控的跨指标方向假设，区分获得支持、与策略矛盾和证据不足，并记录精确 CSV 行、文档行与内容哈希。
- 工作台显示当前数据规模、证据快照编号、文档片段数量和上下文压缩状态；切换数据源会使旧分析失效。
- 公开 ZIP 改为显式包含完整运行时、本地助手适配器和全部场景资源；生成的会话、审计、缓存、测试和用户数据仍被排除。
- 安装包元数据、分析引擎、助手握手和 SkillHub 契约统一升级为 `3.0.0`。

### 修复

- Codex App Server 使用稳定协议字段，并为首次工具冷启动采用独立事件超时。
- Codex 失败任务会保留提供方返回的安全错误原因与错误码，配额或账户问题不再退化为笼统失败。
- 腾讯 WorkBuddy/CodeBuddy 2.115 的 Streamable HTTP POST 响应现已兼容，未登录状态会明确分类，不再显示为普通不可用。
- 泛化追问会继续携带当前确定性分析引用的文档证据，数据源切换后仍会立即丢弃旧结论。
- 390px 布局不再被长选项或网格最小内容宽度撑出横向滚动。

## [2.9.0] - 2026-07-16

### 变更

- 公开展示标题统一为“Data2Doc2Data-面向真实业务的数据+文本循环推理架构”。
- SkillHub 摘要、技能列表元数据、Skill 指令、项目说明和数据连接器指南均改为中文；命令、字段名、第三方产品名和 MIT 法律文本保持原样。

## [2.8.0] - 2026-07-15

### 修复

- 新增本地 SVG favicon 和辅助服务路由，中文工作区启动时不再产生缺失图标请求。

## [2.7.0] - 2026-07-15

### 变更

- 本地辅助界面、内置演示、分析摘要、验证标签和证据说明改用简体中文；使用者自有文档片段与来源路径为保证可追溯性仍保留原文。
- 中文分词现已支持内置的数据→文本→数据验证示例。

## [2.6.0] - 2026-07-15

### 修复

- 公开 ZIP 现在将未经改写的 MIT 正文作为 `LICENSE.md` 纳入，符合 SkillHub 的文件类型策略；源仓库仍保留标准 `LICENSE` 文件。

## [2.5.0] - 2026-07-15

### 新增

- 公开 ZIP 会注入 SkillHub 所需的 `slug`、SemVer 版本、展示名称、摘要、标签和许可证元数据；源 `SKILL.md` 继续保持标准 Codex 格式。

## [2.4.0] - 2026-07-15

### 新增

- 选定的 MIT 许可证已纳入公开 Skill、写入包元数据，并在公开 ZIP 内进行验证。

## [2.3.0] - 2026-07-15

### 修复

- 公开 SkillHub 构建现在要求存在 `LICENSE`；`--draft` 被严格限制为无许可证的本地验证，绝不能发布。

## [2.2.0] - 2026-07-15

### 修复

- 发布构建器会扫描每一个纳入的文本资源；发现禁止的私有标记时拒绝创建公开 ZIP。

## [2.1.0] - 2026-07-15

### 修复

- SkillHub 包采用严格的公开资源扩展名白名单，并排除隐藏文件和符号链接，避免 `.env` 等文件被意外纳入。

## [2.0.0] - 2026-07-15

### 新增

- 新增确定性的 `scripts/build_skill_bundle.py` 命令，生成包含公开 Skill 契约、本地辅助界面、运行时代码与数据连接器指南的 SkillHub 就绪 ZIP。
- 新增回归检查：打开 ZIP 并确认必需的 Skill 文件存在、生成缓存目录缺失。

## [1.9.0] - 2026-07-15

### 变更

- 移除公开 Skill、包说明和运行时错误信息中过时的 `v0.1` 表述，确保版本边界在迭代后仍准确。

## [1.8.0] - 2026-07-15

### 修复

- 保存的本地配置损坏时，现在返回结构化 `422` 响应，而不是关闭辅助服务连接。
- 配置中的来源路径必须是 JSON 文本值，格式错误的配置会在触及文件系统前被拒绝。

## [1.7.0] - 2026-07-15

### 修复

- 超过大小限制的本地文档现在会直接报告大小限制错误，不再被误标为不可读取。
- 自动化回归覆盖真实本地文件的 CSV 与文档字节限制。

## [1.6.0] - 2026-07-15

### 修复

- 本地 HTTP 请求必须使用预期的回环 Host；如包含 Origin，必须为同源 Origin。
- 静态与 JSON 响应现在发送仅限本地的 Content Security Policy、no-sniff、no-referrer 与 no-store 响应头。

## [1.5.0] - 2026-07-15

### 修复

- 辅助服务现在拒绝除 `127.0.0.1` 外的所有 Host。
- 本地输入限制会拒绝超过 5 MB 的 CSV、超过 1 MB 的单个文档，以及包含超过 200 个受支持文档的目录。

## [1.4.0] - 2026-07-15

### 新增

- 新增一条透明的 Data-to-Doc-to-Data 验证规则：可对“激活上升、留存下降”这一文档条件同时检查两条本地 CSV 序列。
- 为辅助指标验证新增专门的结果区块和证据条目。

### 变更

- `supported` 仅用于已确认的双指标规则；其他相关结果保留为 `mixed`。

## [1.3.0] - 2026-07-15

### 修复

- 没有另一项数据验证时，文档匹配不再产生 `supported`；当前相关结果为 `mixed`，并明确其因果限制。
- 验证标题现在用语义样式区分 `supported`、`mixed` 与 `insufficient`，同时保留可见文本。

## [1.2.0] - 2026-07-15

### 修复

- 文档检索现在包含已解析指标，使指标别名可以衔接使用者问题与本地文档。
- 零相关度文档无法再产生 `mixed` 或 `supported` 验证状态。
- 分析限制文本不再嵌入会过时的发布版本号。

## [1.1.0] - 2026-07-15

### 变更

- 分析现在必须先解析指标，不再静默选择无关指标。
- 指标序列少于两条观测值时拒绝分析。
- 命令行与本地界面均接受可选的指标覆盖值。

## [1.0.0] - 2026-07-15

### 新增

- 初始公开且本地优先的 Data2Doc2Data 包。
- 本地 CSV 加 Markdown/TXT 的证据工作流、回环配置页面与合成演示。
- 不包含凭据字段、遥测或网络数据调用。
